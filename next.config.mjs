/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com','https://source.unsplash.com','source.unsplash.com','res.cloudinary.com'],
      },
};

export default nextConfig;
